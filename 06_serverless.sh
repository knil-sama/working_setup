#!/usr/bin/env bash
sudo npm install -g serverless serverless-better-credentials
# serverless config credentials --provider aws --key ${Your access key ID} --secret ${Your secret access key} --profile 